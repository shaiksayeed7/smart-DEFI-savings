# YO Agent - Environment Variables
# Copy this file to .env.local for local development
# Set these in your Vercel project settings for production deployment

# Optional: Anthropic Claude API key for AI-generated allocation rationale
# If not set, the app uses deterministic fallback rationale (fully functional without it)
ANTHROPIC_API_KEY=your_anthropic_api_key_here
